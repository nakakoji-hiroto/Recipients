<% if current_user != user && user.is_active && current_user.followed_by?(user) && user.followed_by?(current_user) %>
  <p class="mt-4"><strong>※<%= user.name %>さんと相互フォロー状態のため、ＤＭ機能が使用可能です！</strong></p>
  <%= link_to 'ＤＭページへ', chat_path(user.id), class: "btn btn-primary border border-dark" %>
<% elsif  current_user != user && user.is_active && current_user.followed_by?(user) %>
  <p class="mt-4 text-success"><strong>※</strong>現在 <strong><%= current_user.name %></strong>さんは、<strong><%= user.name %></strong>さんからフォローされています。<br>
  　<strong><%= user.name %></strong>さんをフォローバックしてＤＭを始めましょう！</p>
<% end %>